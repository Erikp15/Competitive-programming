#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const ll Size=1e3+1;

pair<ll,ll> a[Size];
vector<ll> adj[Size];

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll n;
    cin>>n;
    for(ll i=0;i<n;i++){
        cin>>a[i].first>>a[i].second;
        for(ll j=0;j<i;j++){

        }
    }
    return 0;
}
