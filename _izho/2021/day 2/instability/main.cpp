#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

    ll p[1000001];

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll n;
    cin>>n;
    for(ll i=0;i<n;i++){
        cin>>p[i];
    }
    sort(p,p+n);
    ll total_groups=0;
    do{
        bool is_lit[]
        for(ll i=0;i<n;i++){
            if(i==0){

            }
        }
    }while(next_permutation(p,p+n));
    cout<<total_groups<<endl;
    return 0;
}
/*
2 1
6

4
3 4 2 1
232
*/
