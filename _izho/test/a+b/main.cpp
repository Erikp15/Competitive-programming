#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long double a,b;
    cin>>a>>b;
    cout<<setprecision(6)<<fixed<<a+b<<endl;
    return 0;
}
