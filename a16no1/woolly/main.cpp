#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    string byte_1,byte_2,sum;
    cin>>byte_1>>byte_2;
    ll small_size=min(byte_1.size(),byte_2.size());
    for(ll i=0;i<small_size;i++){
        ll carry=0;
        while(byte_1[byte_1.size()-i-1]!='?' and byte_2[byte_2.size()-i-1]!='?'){

        }
    }
    return 0;
}
