#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

    const ll Size=100001;
    ll a[Size],pre_sum[Size];
    set<ll> num_taken;

    void solve(){
        ll n,q;
        cin>>n>>q;
        for(ll i=0;i<n;i++){
            cin>>a[i];
        }

    }

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll t;
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
}
