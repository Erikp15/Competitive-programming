#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

    const ll mod=998244353,Size=2e5+1;
    ll x[Size],y[Size],dp[Size];
    bool s[Size];

    void solve(){
        ll n;
        cin>>n;
        for(ll i=0;i<n;i++){
            cin>>x[i]>>y[i]>>s[i];

        }
        if(){}
    }

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll t=1;
    while(t--){
        solve();
    }
    return 0;
}
