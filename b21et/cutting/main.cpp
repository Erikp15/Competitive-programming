#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const ll Size=1e3+1;

int state[Size][Size];

void solve(){
    ll n,m,q;
    cin>>n>>m>>q;

}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll t=1;
    while(t--){
        solve()
    }
    return 0;
}
